# Always resolve from the public npm registry. Pinning it here overrides any
# machine-wide ~/.npmrc (e.g. a private mirror), so `npm install` can never
# poison package-lock.json with internal tarball URLs that CI runners can't reach.
registry=https://registry.npmjs.org/

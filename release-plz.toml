[workspace]
changelog_update = true
git_tag_enable = true
# GitHub Release creation is owned by .github/workflows/release.yml
# (triggered by tag push); release-plz only manages version + changelog.
git_release_enable = false
